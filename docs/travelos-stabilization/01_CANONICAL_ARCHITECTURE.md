# 01_CANONICAL_ARCHITECTURE.md
# Arquitetura Canônica (Canonical Architecture)

## 1. Fluxo de Execução da UI
`DESIGN.md` -> `design.css` -> `styles.css` -> `AppShell` -> `Primitivos` -> `Páginas de Módulos`.
Toda a geometria e estilos dependem de tokens herdados dinamicamente do escopo do DOM.
